# Build output. `./build.sh` regenerates all of it.
build/
*.elf
*.map
*.sav

# Vendored engine: a clone of its own, pinned in README.md rather than tracked.
butano/

# Emulator captures and rendered audio previews - large, and regenerated by
# tools/shots.py, tools/gen_music.py and test.sh whenever they are needed.
tools/emu/shots/
tools/emu/shots2/
tools/emu/*.gba
preview/audio/
preview/assets.html
